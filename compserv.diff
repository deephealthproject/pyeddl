diff --git a/src/compserv.cpp b/src/compserv.cpp
index 705abcf..6b88135 100644
--- a/src/compserv.cpp
+++ b/src/compserv.cpp
@@ -35,6 +35,15 @@ CompServ::CompServ(int t, const initializer_list<int> &g, const initializer_list
     local_fpgas = vector<int>(f.begin(), f.end());
 }
 
+
+CompServ::CompServ(int t, vector<int> g, vector<int> f) {
+    type = "local";
+    local_threads = t;
+    local_gpus = g;
+    local_fpgas = f;
+}
+
+
 // for Distributed
 CompServ::CompServ(FILE *csspec) {
 
diff --git a/src/compserv.h b/src/compserv.h
index 6a602db..97cf847 100644
--- a/src/compserv.h
+++ b/src/compserv.h
@@ -40,6 +40,8 @@ public:
     // for local
     CompServ(int threads, const initializer_list<int> &g, const initializer_list<int> &f);
 
+    CompServ(int t, vector<int> g, vector<int> f);
+
     // for Distributed
     explicit CompServ(FILE *csspec);
 
